# fledge.toml: project task definitions
[tasks]
format = "cargo fmt --check"
lint = "cargo clippy -- -D warnings"
test = "cargo test"

[lanes.verify]
description = "Format, lint, and test the coverage plugin"
steps = ["format", "lint", "test"]
